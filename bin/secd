#!/usr/bin/env ruby
# -*- coding: utf-8 -*-
$:.unshift File.dirname(__FILE__) + "/../lib"
require 'secd'

ctx = Secd::Context.new
ctx.control.unshift(Secd::Add.new(ctx), 3, 4)
vm = Secd::Vm.new(ctx)
vm.run
puts vm.stop
